name: CI
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get you code
        uses: actions/checkout@v2
      - name: Install packages
        run: yarn
      - name: build
        run: yarn build-and-debug
# Base URL for OAuth server metadata
# Set this to your production domain when deploying
BASE_URL=https://your-domain.com

# Production environment
NODE_ENV=production

# Redis configuration
# For local development with docker-compose, use: redis://redis:6379
# For production, use your Redis instance URL
REDISCLOUD_URL=redis://localhost:6379

# Redis TTL settings (in seconds)
# OAuth tokens: 1 hour default
REDIS_TTL_TOKEN=3600
# Auth codes: 10 minutes
REDIS_TTL_AUTH_CODE=600
# OAuth clients: 30 days
REDIS_TTL_CLIENT=2592000
# Session transports: 7 days (matching current session duration)
REDIS_TTL_SESSION=604800